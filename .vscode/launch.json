{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI: HACKALEM AI",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "cwd": "${workspaceFolder}/backend",
      "env": { "PYTHONPATH": "${workspaceFolder}/backend" },
      "args": ["app.main:app", "--reload"]
    }
  ]
}

